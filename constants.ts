/**
 * Global Constants
 */

export const __DEV_MOCK_MODE__ = false;

export const MAP_CONFIG = {
    DEFAULT_DELTA: 0.05,
    MIN_ZOOM: 10,
    MAX_ZOOM: 20
};

export const SESSION_CONFIG = {
    WATCH_TIME_THRESHOLD_MS: 30000, // 30s
    SKIP_THRESHOLD_MS: 2000,       // 2s
};
